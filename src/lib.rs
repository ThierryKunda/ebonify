#[cfg(test)]
mod tests {
    use std::fs;

    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }

    // #[test]
    // fn seperateLines() {
        
    // }
}